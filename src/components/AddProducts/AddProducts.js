import React from 'react';
import Header from '../Header/Header';

const AddProducts = () => {
    return (
        <div>
            <Header />
            <h1>AddProducts</h1>
        </div>
    );
};

export default AddProducts;